import { ReactComponent as CropCorner } from "~/assets/crop-corner.svg";

const CropCornerIcon = ({ className }) => {
  return <CropCorner className={className} />;
};

export default CropCornerIcon;
